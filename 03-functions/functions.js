// Regular function
function greet(name) {
  return `Hello, ${name}!`;
}

// Arrow function
const calculateTotal = (price, quantity) => price * quantity;

console.log(greet("WebPrims Student"));
console.log("Total:", calculateTotal(499, 3));

// Practice: create a function that calculates a discount percentage.
